# CULT on Render — web + managed Postgres
# Docs: https://render.com/docs/blueprint-spec

databases:
  - name: cult-db
    plan: free
    databaseName: cult
    user: cult

services:
  - type: web
    name: cult-web
    runtime: node
    plan: free
    region: oregon
    buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm db:push && pnpm db:seed && pnpm build
    startCommand: pnpm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PORT
        value: "10000"
      - key: DATABASE_URL
        fromDatabase:
          name: cult-db
          property: connectionString
      - key: AUTH_SECRET
        generateValue: true
      - key: AUTH_TRUST_HOST
        value: "true"
      - key: NEXTAUTH_URL
        sync: false
      - key: BLOB_STORAGE_DIR
        value: /opt/render/project/src/.data/blobs
